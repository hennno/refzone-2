import { Auth } from '@supabase/auth-ui-react';
import { ThemeSupa } from '@supabase/auth-ui-shared';

// This is just a test file to verify imports work
console.log('Auth component:', Auth);
console.log('ThemeSupa:', ThemeSupa);